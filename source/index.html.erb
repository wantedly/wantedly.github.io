---
pageable: true
per_page: 10
---
<% if paginate && num_pages > 1 %>
  <p>Page <%= page_number %> of <%= num_pages %></p>

  <% if prev_page %>
    <p><%= link_to 'Previous page', prev_page %></p>
  <% end %>
<% end %>

<% page_articles.each_with_index do |article, i| %>
    <div class="page-header">
      <h2>
        <a href="<%= "https://www.wantedly.com/users/#{article.data.wantedly_id}" %>">
          <img src="https://graph.facebook.com/<%= article.data.facebook_id %>/picture" />
        </a>
        <%= link_to article.title, article %>
        <br/>
        <small><%= article.date.strftime('%b %e') %></small>
      </h2>
    </div>
    <!-- use article.summary(250) if you have Nokogiri available to show just the first 250 characters -->
    <div class="article">
      <%= article.body %>
    </div>
<% end %>

<% if paginate %>
  <% if next_page %>
    <p><%= link_to 'Next page', next_page %></p>
  <% end %>
<% end %>
